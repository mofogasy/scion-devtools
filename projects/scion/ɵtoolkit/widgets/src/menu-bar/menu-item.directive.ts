import { Directive } from '@angular/core';

/**
 * Use this directive to model a menu item for {SciMenuBarComponent}.
 * The host element of this modelling directive must be an <a>.
 *
 * ---
 * Example usage:
 *
 */
@Directive({selector: 'ng-template[sciMenuItem]'})
export class SciMenuItemDirective {
}
